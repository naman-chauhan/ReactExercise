import React from "react";
import PostItem from "./postItem";
const Post = ({ post, index, addPosts }) => {
    return (
        <div>
            <PostItem title1={props.title} />
            <PostItem title2={props.title} />
        </div>
    );
};

export default Post;
